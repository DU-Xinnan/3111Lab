@model IEnumerable<SinExWebApp20256461.Models.PakageTypeSize>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PackageType.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.size)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.weightLimit)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PackageType.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.size)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.weightLimit)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PakageTypeSizeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PakageTypeSizeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PakageTypeSizeID })
        </td>
    </tr>
}

</table>
